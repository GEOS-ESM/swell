window begin: '{{window_begin_iso}}'
window length: ${window_length}
forecast length: ${window_length}
geometry:
  ${GEOMETRY}
model:
  ${MODEL}
initial condition:
  '{{BACKGROUND}}'
observations:
  get values:
    ${MODELGETVALUES}
  observers:
    ${OBSERVATIONS}
