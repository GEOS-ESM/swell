# TODO: This will change with get_background tasks
fetch:
  an:
    - file_type: MOM.res
      filename: '{{cycle_dir}}/MOM6.res.{{local_background_time}}.nc'
  fc:
    - file_type: MOM.res
      filename: '{{cycle_dir}}/MOM6.res.{{local_background_time}}.nc'
store:
  fc:
    - file_type: MOM.res
